===============================================================================
GOODGROUND — CONTEXT HANDOFF
Last updated: 2026-08-24. Since the 2026-08-13 entry below (§26), the site
went through a second homepage promotion and a fresh SEO audit cycle:
  2026-08-20  Homepage promoted from the /home-test-2 A/B variant, replacing
              the section set §20-26 describe. Fonts also changed again:
              Parkinsans (headings) + Instrument Sans (body), replacing Syne
              + DM Sans from §19. See §27.
  2026-08-23  Content/em-dash sweep, dead-code cleanup (old Hero.tsx and
              friends deleted), first-ever DESIGN.md generated via
              `/impeccable document`. See §28.
  2026-08-24  Full SEO audit re-run; Google Business Profile confirmed real
              and mostly healthy (was previously assumed not to exist);
              four fixes shipped and pushed live: homepage FAQ schema
              duplication, case-study video eager-load, cookie-banner
              covering the mobile sticky CTA bar, framer-motion removed
              from the root layout (was only supposed to be on /about and
              /pricing). See §29-§30.
See §20-§30 in section 8 for full detail on each entry. Section 2 below has
been updated in place for the font change; the CONTRAST RULES from §24 are
still current and load-bearing — read them before touching any colour.
EVERYTHING IN SECTION 8 BELOW §16 (historical archive, pre-2026-08-06) IS
SUPERSEDED for design-system specifics — palette, layout, component names
all changed in the 2026-08-06 site-wide redesign, and again in the
2026-08-20 homepage promotion (§27). Business facts there may also be
stale; sections 1/6 below are the corrected current facts.
===============================================================================

0. READ FIRST
--------------
This file is the living session-level handoff. Section 1 = what the site is
today. Section 2 = the most recent work (this is what a fresh session should
read first if picking up where this left off). Section 5 = rules that cause
real rework if violated. Section 8 = compressed history of everything before
the 2026-08-06 redesign — useful for archaeology, not for current facts.

Companion memory files (persistent, cross-session, outside this repo) exist
in Claude's own memory system under the key `project_goodground_website` and
several `feedback_*`/`reference_*` files — a Claude session with memory
access already has this context loaded automatically. This .txt file is for
continuity in a session WITHOUT that memory (a fresh machine, a different
tool, a human reading it directly).


1. PROJECT — CURRENT STATE
----------------------------
GoodGround is a website development studio in George, on the Garden Route
(Western Cape), South Africa. Founded 2026. Its own registered company:
  legalName: GoodGround (Pty) Ltd
  registrationNumber: 2024/641034/07   (confirmed by founder 2026-07-23 —
    NOT "no registration number, trades under The Trail Tribe" as an older
    version of this file said; that was true once, is not true now)
  Not VAT registered — all displayed prices are VAT inclusive.
Founder: Johandre Saayman (never named on the site itself, by his explicit
request — About page describes him by experience only). He also runs Grow
Brand Agency and the separate "Ground Ops" app (different project, different
repo — see Claude's `project_groundops_status` memory, do not confuse them).

Location on disk:
  c:\Users\Johandre\Desktop\GoodGround\GroundOps-Work\website-and-ops\
  goodground-sites\site\
  (This is a DIFFERENT path than older notes in this file reference —
  the `c:\Volumes\growbrand\...` path in section 8 is stale.)

Repo:  https://github.com/goodground-za/goodground-site  (PUBLIC)
Live:  https://www.goodground.co.za   <- canonical, serves 200
       https://goodground.co.za       <- 308-redirects to www
Deploy: push to `main` -> Vercel (Hobby plan) auto-deploys. Johandre says
  "push to main" / "push to live" as the trigger phrase each time.

Stack: Next.js 16 (App Router, Turbopack) + TypeScript + Tailwind v4 (no
config file — tokens live in `@theme` in app/globals.css) + GSAP/Lenis for
motion (Framer Motion survives only in one small component, see below). npm.
No CMS — content lives in typed files under `/content`, a hard client
requirement carried through the whole project.

REPO NOTE: this is Next 16, which has real breaking changes vs a model's
training data on "Next.js." Check `node_modules/next/dist/docs/` for
anything unfamiliar (App Router conventions, opengraph-image,
generateStaticParams) before assuming older Next.js patterns apply.


2. CURRENT DESIGN SYSTEM (since 2026-08-06 site-wide redesign)
------------------------------------------------------------------
The whole site was re-skinned in one pass 2026-08-06, promoting an approved
`/home-test` A/B variant to be the site's one design language (that route no
longer exists — it was promoted, not kept as a separate page).

Palette (Tailwind v4 @theme tokens, app/globals.css):
  --color-ht-purple  #2e1848   dark sections, footer, PageHero band
  --color-ht-orange  #fe431a   primary action FILLS / CTAs
  --color-ht-pink    #fe9dd2   accents, card rings
  --color-ht-crimson #ac3026   accent TEXT on light surfaces; hover depth
  --color-ht-cream   #fbf7ec   base page background
  --color-ink        #010602   TEXT on orange fills
Radii: --radius-block 40px, --radius-card 24px, --radius-pill 999px
Fonts: Parkinsans (`font-ht-display` — headings/buttons/labels only) +
  Instrument Sans (`font-ht-body` — body copy). Loaded once in app/layout.tsx
  via next/font/google. (Superseded Syne + DM Sans on 2026-08-20, §27.) The
  Satori OG-image renderer needs its own static-weight TTF pinned at wght 700
  (app/_og/Parkinsans-Bold.ttf), since Google only ships this font as a
  variable font and Satori renders a variable font at its default axis
  position (300/Light) otherwise.

*** CONTRAST RULES — LOAD-BEARING, READ BEFORE TOUCHING ANY COLOUR ***
ht-orange is a bright fill and fails WCAG AA easily. These four rules were
all being violated in production until 2026-08-13 (§24), which held
accessibility at 96. THE SURFACE DECIDES THE TOKEN:
  text on an ORANGE fill        -> text-ink          (5.86:1)  NOT white (3.48)
  accent text on CREAM/WHITE    -> text-ht-crimson   (6.08:1)  NOT orange (3.25)
  accent text on PURPLE         -> text-ht-orange    (4.50:1)  NOT crimson (2.41)
  muted body text bottoms out at text-ht-purple/70   (5.78:1)  NOT /60 (4.22)
Two deliberate exceptions, do not "fix" them:
  - Display text >= 18.66px bold MAY stay white on orange: WCAG's large-text
    bar is 3:1 and 3.48 clears it. CTABand's headline and the article CTA h2
    rely on this.
  - Button.tsx `outline` variant keeps ORANGE text because it has a
    transparent fill and only ships on the dark cookie banner. Crimson there
    would be 2.41:1. This one bit during the sweep and Lighthouse caught it.
If you bulk-change these colours: sed the tree, then RE-RUN LIGHTHOUSE, and
expect one or two dark-surface exceptions to need reverting.

Motion: GSAP + Lenis smooth-scroll is the primary system sitewide, in
`components/motion-gsap/*`:
  RevealSection / RevealStagger  — scroll-triggered fade+rise entrances
  SplitWords                     — word-by-word headline reveal
  MagneticButton                 — cursor-follow drift on buttons/links
  HoverCard / HoverCardImage     — card lift + independent image drift
All of these gate on `gsap.matchMedia("(prefers-reduced-motion: no-preference)")`
— under reduced motion, elements simply render at their natural DOM state
(no opacity:0 stuck on screen), by construction. Framer Motion
(`framer-motion`) is now genuinely minimal: `components/sections/ParableModal`
(used on /about) and `components/motion/BorderTrail.tsx` (used on /pricing)
are the only two consumers, confirmed 2026-08-24 (§30) after finding and
removing a third, undocumented one in the cookie-consent banner — CHECK
IMPORTS BEFORE TRUSTING THIS LIST, it undercounted for a while.

Recurring visual conventions worth knowing before touching any page:
  - "Hard-shadow" card: `rounded-card ring-2 ring-ht-pink
    shadow-[0_14px_0_0_var(--color-ht-pink)]` (colour varies by surface —
    purple ring/shadow on light cards, pink on white cards, etc.)
  - Every inner page opens on a dark ht-purple band
    (`components/PageHero.tsx`) — load-bearing, because `Nav.tsx` is
    `fixed` + transparent-until-scroll and needs a dark surface under it for
    contrast. Do not build a new page that skips PageHero.
  - CloudDivider scallop pattern for section transitions into the Footer;
    whatever section sits directly above `<Footer>` needs `pb-[24vw]`
    reserved so the divider's circles don't collide with card shadows.
  - `min-w-0` is required on every `grid lg:grid-cols-N` column that holds
    long-form/constrained-width text, or CSS Grid's default min-content
    sizing silently breaks the layout at narrow widths. This bit multiple
    pages during the redesign; check for it if a grid column looks wrong.
  - Nav/Footer are global (`components/Nav.tsx`, `components/Footer.tsx`),
    same on every route — no per-page nav/footer swapping.

Hard rules carried over from before the redesign, still in force: no
fabricated stats/quotes/logos/reviews; no em dashes in user-facing copy
(code comments are exempt); founder never named on the site; "X, not just
Y" is deliberate brand voice, not an AI tell — do not "fix" it.


3. LATEST WORK: /pricing PAGE (2026-08-10 to 2026-08-11, LIVE)
--------------------------------------------------------------------
Commits (chronological):
  82feeaf  Add /pricing page with fixed packages and Build Your Own
           configurator (also: nav restructure, contextual links)
(Earlier same arc, already landed before this handoff was last touched:
  16c3ba1  Add real photos: mobile-only hero crop, About founder image
  6bd32c3  Fix contact/start-project forms: CSP connect-src blocked
           Web3Forms fetch
  ac4d575  Fix carousel z-index glitch, round About's Why-the-name card,
           center CTA, add FAQ spacing)

WHAT SHIPPED: a brand-new pricing/packages page — real, client-confirmed
numbers (not placeholder), likely the single most important conversion page
on the site per the founder's brief.

Content — `content/pricing.ts`, verbatim from the founder's own document,
do not alter figures without him supplying new ones:
  4 fixed packages: Micro R8,500 / Launch R15,000 / Grow R23,000 /
    Scale R32,000 — each with pitch, feature list, "Best for", timeline.
  Base Build Fee R6,000 + a 7-category a la carte menu (Pages, Design,
    Functionality & Interactivity, Integrations, SEO & Performance,
    Content, Extras) — see the file for every item/price.
  `MONTHS = 12` and `monthlyFor(total)` helper — EVERY price on this page
    shows both the one-time total and a /mo-over-12-months figure. This was
    an explicit decision (Johandre chose "show monthly-equivalent too")
    to keep the page consistent with the sitewide "12 equal monthly
    payments, no large upfront cost" differentiator used everywhere else.
  `pricingFaq` array — 5 Q&As, flagged DRAFT in a comment in the file.
    Claude wrote these (grounded only in already-confirmed facts: revision
    counts per package, the 12-month split, "free quote before work
    begins", the "1-2 business days" reply time already used elsewhere).
    **STILL NEEDS JOHANDRE'S SIGN-OFF** — this is the top open item.

Components (all new):
  app/pricing/page.tsx              — server component, has `metadata`,
                                       PageHero, delegates interactivity
  components/PricingPageClient.tsx  — client component, owns the single
                                       `selectedConfig` state (whichever
                                       package or a-la-carte build the
                                       visitor last picked), scroll-to-form
                                       logic, renders the purple bridge
                                       section between Packages and the
                                       configurator
  components/sections/PricingPackages.tsx     — 4-card grid, "Grow" is
    flagged "Most Popular" (Johandre's explicit editorial call, not
    inferred from data — if this ever needs to move/change, ask him first)
  components/sections/PricingConfigurator.tsx — a-la-carte category
    accordions (same GSAP height-tween pattern as ServiceAccordion/
    FAQAccordion) + live summary panel. Desktop: sticky sidebar. Mobile:
    fixed bottom bar, gated by IntersectionObserver so it only shows while
    the configurator section itself is in view (without that gate it stays
    pinned over the footer for the rest of the scroll — a real bug caught
    and fixed this session). Has a "proximity nudge" (within ~15% of a
    package price -> suggests that package) AND an "exceeds nudge" (total
    has passed even the biggest package -> different message) — both
    computed live from the `packages` array, no extra data needed.
  components/sections/PricingFAQ.tsx          — pricing-specific FAQ
    accordion (DRAFT content, see above)
  components/PricingEnquiryForm.tsx           — modeled on ContactForm.tsx,
    same validation/sendEnquiry() path via lib/enquiry.ts. Message field is
    OPTIONAL (was required in the first pass — Johandre's page copy says
    "or nothing at all," so requiring a message contradicted the page's own
    promise; caught and fixed same session). Renders a read-only "Your
    configuration" summary above the fields when selectedConfig is set.
  components/AnimatedNumber.tsx     — new reusable primitive: GSAP
    count-up-price tween (proxy-object tween + textContent write per tick,
    NOT a raw number tween on textContent, which would print fractional
    rand mid-flight), gated by gsap.matchMedia the same way as everything
    else. Used everywhere a price appears on this page.

Nav change (Johandre's explicit instruction, 2026-08-11): "Pricing" was
REMOVED from the top nav (`content/site.ts` navLinks) — it stays in
footerLinks only. In its place, contextual links to /pricing were added
where they make sense:
  - Services page's purple "payment recap" section CTA now says "See
    Pricing ->" and points to /pricing instead of /contact.
  - Homepage Hero's micro-trust line — the "12 equal monthly payments" item
    specifically — is now a link to /pricing.
  - About page's closing "How we work" paragraph now also links to
    "view pricing" alongside its existing "See the full process" link.
SIGNAL FOR FUTURE PAGES: this establishes the site's IA preference as a
lean top nav. Default new secondary pages to footer + contextual links, not
automatic top-nav inclusion, unless told otherwise.

Also touched: app/sitemap.ts (added /pricing, priority 0.95, just under the
homepage).

VERIFICATION DONE THIS SESSION (all via Chrome DevTools MCP + a local dev
server on port 3210, plus `npx tsc --noEmit` / `npx next build` clean):
  - Two-pass screenshots at 1440x900 / 820x1180 / 390x844.
  - Clicked through the full configurator: item toggles update the running
    total live with the count-up animation; proximity nudge fires exactly
    at a package price threshold (verified at R8,500 = Micro); exceeds
    nudge fires past R32,000 (verified at R32,600); mobile sticky bar
    appears/disappears correctly tied to section visibility; both CTA paths
    (a package card, and the configurator's "Get This Quote") correctly
    scroll to and pre-fill the enquiry form.
  - prefers-reduced-motion verified via `navigate_page`'s `initScript` to
    override `window.matchMedia` (chrome-devtools MCP's `emulate` tool has
    no direct reduced-motion flag) — content renders immediately and
    correctly with no stuck/fading state.
  - ONE real end-to-end enquiry submission ("Test User" / test@example.com)
    to confirm the live Web3Forms POST succeeds with the new "Your
    configuration" field. This is a REAL email in the studio inbox — ignore
    or delete it, same pattern as the earlier contact-form-fix session.
  - No console errors at any breakpoint.

GOTCHA HIT AND ROOT-CAUSED THIS SESSION (worth knowing for any future
visual-verification work on this site): `take_screenshot` with
`fullPage: true` can render entire below-the-fold sections as blank/missing
on this site, even though the content is real and correctly sized in the
DOM. Cause: fullPage screenshots instantly resize the viewport to full
document height instead of scrolling through it, and this site's GSAP
ScrollTrigger reveals (RevealSection/RevealStagger) never receive the
normal scroll events that would fire their reveal tween, so they stay stuck
at their initial `gsap.set(el, {opacity: 0, ...})` state. FIX: scroll to a
specific position via `evaluate_script` + `window.scrollTo`, then take a
non-fullPage (viewport) screenshot at that position instead. Do not
diagnose a "content vanished" fullPage screenshot as a real bug on this
codebase before checking with a normal scroll first.

ALSO FIXED THIS SESSION (unrelated to pricing, but same session): Chrome
DevTools MCP was found completely deregistered on this machine
(`~/.claude/.mcp.json` was `{"mcpServers": {}}`, empty) despite old
permission grants for its tools still sitting in
`~/.claude/settings.json`. Re-registered:
  { "mcpServers": { "chrome-devtools": { "command": "npx",
    "args": ["-y", "chrome-devtools-mcp@latest"] } } }
Requires a Claude Code restart/reload to take effect. If this ever
disconnects again on this machine, check that file first.


3b. LATEST WORK: TWO PAYMENT OPTIONS, À LA CARTE UPDATE, SHARE BUTTONS
------------------------------------------------------------------------
All shipped 2026-08-11, same day as the /pricing page above, in three
separate commits.

TWO PAYMENT OPTIONS (commit 8b8360d) — the sitewide "12 equal monthly
payments, no large upfront cost" promise is RETIRED. Replaced everywhere
(Hero trust line, /pricing, /services, /about, content/faq.ts,
content/stats.ts, content/articles.ts, components/Schema.tsx JSON-LD,
app/layout.tsx + per-page metadata, public/llms.txt — ~20 files) with two
real options:
  - 50% deposit to secure the project, 50% on completion, before handover.
  - 12 equal monthly instalments — the first secures the booking, the
    remaining 11 become payable once the project is COMPLETED (not spread
    across the build — a real behavioural difference, not just new copy).
  - Cancellation (confirmed with Johandre): same rule as before, applied
    to both — outstanding balance of the agreed price becomes payable, not
    a refund of what's paid. Do not invent a different cancellation policy.
New helpers in content/pricing.ts: depositFor, balanceFor,
monthlyInstalmentFor (replaced the old monthlyFor/MONTHS pair). Every price
on /pricing now shows both breakdowns (e.g. "R11,500 deposit + R11,500 on
completion" / "or R1,917/mo x 12").

THE SAME CHANGE WAS ALSO BUILT INTO GROUNDOPS (the quoting/invoicing app,
a separate repo) the same session — quotes get a payment_option field that
carries through to invoices; converting a quote creates one correctly-
computed first invoice (deposit, or instalment 1 of 12); staff create every
subsequent payment manually (no auto-generation). CONFIRMED WORKING by
Johandre after running the required migration — see that repo's own
groundops_handoff.txt sections 14-15 for full detail, this is a two-repo
feature.

À LA CARTE MENU UPDATE (commit c9d778f) — Build Your Own menu
(content/pricing.ts, components/sections/PricingConfigurator.tsx):
  - "Additional standard page" (R2,500/ea) and "Additional blog post"
    (R600/ea) changed from a single on/off toggle to a real +/- quantity
    stepper (AlaCarteItem.quantifiable: true) — visitors can now add more
    than one. Configurator state changed from a Set<string> of selected
    ids to a Record<string, number> of quantities.
  - Custom logo design R3,500 -> R7,500, Custom illustrations/graphics
    R1,500 -> R2,500, Custom icon set R1,800 -> R2,500 — all flagged
    startingAt: true, rendered as "from RX" (final price depends on
    complexity, confirmed in the quote).
  - REMOVED from the menu entirely: "Local SEO setup (Google Business
    Profile optimisation)" and "Professional photo editing (per image, up
    to 10)".

BLOG SHARE BUTTONS (commit f7ae898) — new components/ShareButtons.tsx,
shown twice on every article page (app/insights/[slug]/page.tsx): next to
the byline on the purple hero band, and again above the closing CTA.
WhatsApp, X, Facebook, LinkedIn (solid-glyph SVGs — stroke/outline icons
were tried first and were unrecognizable at 16px, especially WhatsApp) plus
Copy Link (clipboard API, 2s confirmation state). WhatsApp listed first
deliberately — it's the dominant share channel for this South African
small-business audience, and explicit icons are used rather than gating
behind the Web Share API since navigator.share isn't supported on desktop
Safari/Firefox.

GIT PUSH GOTCHA RESURFACED: pushing the share-buttons commit hung with no
output for the full 2-minute tool timeout — same symptom as the historical
git-credential-manager GUI-dialog hang documented in section 9's archive
(the fix there: `git -c credential.helper= -c credential.helper=store push
origin main`). A plain retry (`git push origin main` again, no special
flags) succeeded immediately this time, so it isn't consistently
reproducible — try a bare retry first before reaching for that workaround.
UPDATE: the hang recurred a second time in this same session (pushing the
handoff-doc update just above) and that time the credential-helper reset
was actually needed to clear it. Treat this as genuinely recurring, not a
fluke — always wrap `git push` in a shell `timeout` (20-30s) rather than
letting it run un-timed, try a plain retry first, then the reset if that
fails too.


3c. NEW BLOG ARTICLE: "WEBSITE VS FACEBOOK PAGE" (commit 25c1f58)
--------------------------------------------------------------------
content/articles.ts — new entry prepended to the `articles` array (newest
first; the first entry auto-becomes the featured post on /insights and the
newest slug picked up by generateStaticParams, no other file needs
touching to publish a new article). Slug website-vs-facebook-page, category
Growth, ~1,680 words, 9 min read.

Johandre asked for this one with a specific voice brief: write as a strict
copy chief trained on David Ogilvy's principles, exclusively active voice,
highly specific language over vague/generic wording, no filler words or
weak adverbs, minimum 1,500 words, keep SEO and internal linking in mind.
WORTH REUSING THIS BRIEF VERBATIM if he asks for another article without
repeating the instructions — this is now the expected bar for blog content
on this site, not a one-off request.

Process followed, worth repeating for future articles:
  - Read the full content/articles.ts file first to match the established
    Block-array content model (p / h2 / ul / quote) and the site's own
    already-Ogilvy-adjacent voice (short declarative sentences, no em
    dashes, arguments made from mechanism rather than invented statistics
    — this project's no-fabrication rule already pushed the existing
    articles toward exactly the voice Johandre asked for).
  - No fabricated stats, client anecdotes, or specific incidents — argued
    every claim from mechanism (how Facebook's algorithm/ownership model
    actually works) rather than invented numbers or a "a client of ours
    once..." story.
  - Internal links use the `[label](/path)` syntax inside any block's
    `text` field, parsed by `withLinks()` in
    app/insights/[slug]/page.tsx — the ONLY way to link from article prose,
    since the content model is plain typed data, not JSX. This article
    links twice to the existing "why small businesses in South Africa need
    a website" piece (which already covers the "rented ground" argument,
    extended here specifically for Facebook), plus /pricing and
    /start-project.
  - Ran the full draft through the `humanizer` skill's AI-writing-tell
    checklist before shipping (CLAUDE.md step 13, mandatory before any
    user-facing content is called final) — found and fixed one triple-
    adjective stack ("quick, timely, low-stakes" -> "fast, low-stakes"),
    otherwise the draft was already clean against every pattern on that
    checklist (no em dashes, no significance inflation, no vague
    attributions, no signposting, no fragmented headers, no generic
    "the future looks bright" closer).
  - Verified word count with a quick throwaway Node script parsing the
    `text:`/`items:` fields directly out of the TS source (1,679 words) —
    faster than trying to import a .ts file at runtime.

VERIFIED: tsc clean, next build clean (new route generated correctly,
including its own opengraph-image), visually confirmed on /insights
(featured slot) and the article page itself (both share-button rows
present, all 4 internal links resolve, heading/date/category correct).


4. OPEN ITEMS
---------------
  0. **CLIENT PROJECTS FOR /work** — Johandre said 2026-08-13 he'd be adding
     a few real projects "in the next week". /work is now a project list
     driven by `content/projects.ts`; adding one is a content change only,
     no page code. First entry gets the big feature card, the rest fill the
     grid. Fill in every field and only add a project once it is genuinely
     live. THIS IS THE NEXT EXPECTED PIECE OF WORK.
  1. ~~Pricing FAQ sign-off~~ RESOLVED 2026-08-13 (§24). The 5 `pricingFaq`
     answers were rewritten, em dashes removed, and the DRAFT markers
     cleared. Three other DRAFT-flagged items that were also live in
     production (homepage "Creative Strategy" card, two ServiceCarousel
     cards) were rewritten in the same pass. No DRAFT markers remain in
     content/ — `grep -rn "DRAFT" content/` should stay empty.
  2. **Google Business Profile / NAP check (needs Johandre)** — the phone
     number now feeds `telephone` in LocalBusiness schema. That only helps
     local ranking if GBP lists the IDENTICAL number in the same format
     (+27 67 010 4988). Mismatched NAP between site and GBP actively hurts.
     Also still outstanding: confirm the sitemap is submitted in Search
     Console and check the Coverage report — needs his login, cannot be
     done from a session.
  3. Everything in the OLD section 5 of this file (now folded into section 8
     as history) listed several founder-level open decisions from earlier
     sessions (named article author, /work case study, phone number, GBP,
     real photography, social URLs). The phone number and /work case study
     are now resolved (see above and §26). The rest: STATUS UNKNOWN — a
     fresh session should re-check current state (content/site.ts,
     content/articles.ts) rather than assume. This file was not kept
     continuously updated between 2026-07-30 and 2026-08-06+, so there is a
     real gap in the record there.
  3. **Favicon/app-icon refresh (optional, not urgent)** — app/icon.png,
     apple-icon.png, public/favicon.ico are a neutral black-circle/grey "gg"
     mark, not colored to either the old or the new palette. They don't
     clash with anything. Only worth regenerating if Johandre wants the
     browser tab to actively carry the brand orange/purple rather than stay
     neutral. The site's actual logo files (public/logos/*.svg) needed no
     changes for the redesign and need none now — no hardcoded color, always
     forced white via CSS at every call site.
  4. No further design/dev work is queued beyond the above as of
     2026-08-11.
  5. **PRICING VISIBILITY ON HOMEPAGE (flagged by Johandre 2026-08-24 as the
     top priority, not yet started)** — PRODUCT.md names public fixed
     pricing as the site's core wedge/differentiator, but the current
     (post-2026-08-20) homepage does not clearly surface it. This is the
     next piece of work to pick up.
  6. **GBP dashboard fixes (needs Johandre, not code)** — confirmed 2026-08-24
     (§29): hours currently show "Open 24 hours" Mon-Fri / "Closed" Sat-Sun,
     which looks like an unset default, not real hours. Also worth checking
     whether a more precise GBP category than "Design agency" is available
     (e.g. "Website designer") — category is reportedly the single
     highest-weighted local ranking factor.
  7. **Remaining SEO Medium/Low items** — see seo-audit/ACTION-PLAN.md, but
     note that file is from the 2026-08-17 audit round and is stale relative
     to the 2026-08-24 re-run (§30); treat it as historical reference, not a
     current backlog, before acting on any item in it.


5. STANDING RULES (violating these has caused real rework, repeatedly)
--------------------------------------------------------------------------
  - NO EM DASHES in user-facing copy. Commas, full stops, or a colon
    instead. Code comments/aria-labels are exempt.
  - "X, not just Y" is deliberate brand voice, not an AI tell — leave it.
  - THE FOUNDER IS NOT NAMED ANYWHERE ON THE SITE (his explicit request).
  - No fabricated content, ever — no invented stats, quotes, logos,
    reviews, prices, feature lists, or business policy specifics (e.g. a
    deposit percentage). If real content is referenced but not supplied,
    ASK how to get it (paste it, point to a file) rather than inventing a
    placeholder and moving on — this has come up multiple times (pricing
    content itself was sourced this way) and is the established norm here.
  - Do not invent a phone number, registration number, or co-founder.
  - Confirm before any risky/irreversible action (force-push, deleting
    content) — routine "push to main" for reviewed, verified work is
    already pre-authorized by the founder's own repeated instruction to do
    so, but say what you're about to push before doing it if the change is
    large.
  - Two-pass screenshot verification (1440x900 / 820x1180 / 390x844) for
    any visual change, saved to `.screenshots/<task-slug>/` — that folder
    is gitignored and user-owned, never auto-delete it.


6. FACTS AND SPECIFICS
-------------------------
Business:
  legalName:          GoodGround (Pty) Ltd
  registrationNumber: 2024/641034/07
  VAT:                not registered; all prices VAT inclusive
  Email:               hello@goodground.co.za
  Location:            George, Garden Route, Western Cape, South Africa
  Socials:             Facebook + Instagram, both live (see content/site.ts)
  Phone:               067 010 4988 / +27670104988 (confirmed by Johandre
                        2026-08-13, WhatsApp-capable).
                        *** IT MUST NOT BE DISPLAYED. *** His explicit
                        instruction: steer people to email, he does not want
                        random calls/WhatsApps. Implementation:
                          - `site.phone` STAYS null ON PURPOSE. Footer.tsx
                            renders a tel: link from it, so setting it would
                            print the digits on every page. Do not "fix" it.
                          - the number lives in `site.whatsapp` instead
                            ({ e164, link }) and is used in exactly two
                            places: a click-to-chat button on /contact, and
                            `telephone` in the LocalBusiness JSON-LD.
                          - verified: zero visible digits and zero tel:
                            links sitewide.
                        HONEST CAVEAT: it IS present in the page source, in
                        the JSON-LD block, and in content/site.ts in this
                        PUBLIC repo. That is the unavoidable cost of the
                        local-SEO benefit. The protection is against casual
                        harvesting of visible text and against one-tap
                        dialling, not against a determined scraper.

Technical:
  GA4 ID:            G-T4JF7EJLW9 (env var NEXT_PUBLIC_GA_ID, Vercel
                      Production only) — consent-gated, does not mount
                      until the visitor accepts the cookie banner.
  Web3Forms key:      beae6de4-74ed-4868-a4eb-e479e0f43e6c (hardcoded
                      fallback in lib/enquiry.ts; Web3Forms keys are public
                      by design). Env var override still works if set.
                      DO NOT reuse Grow Brand Agency's key — different
                      inbox.
  CSP note:           `connect-src` (not `form-action`) governs fetch()
                      calls — this distinction caused a real bug (forms
                      silently failing) earlier this project; both are set
                      correctly now in next.config.ts, but if a new
                      external API is ever called via fetch from a form,
                      remember to add it to connect-src too.
  DNS:                Afrihost. Root A -> Vercel. www CNAME -> Vercel.
  GitHub push identity: has hung on credential-manager before (see section
                      8 for the historical fix, `-c credential.helper=`
                      reset) — not re-verified as still an issue on this
                      machine/session, but keep in mind if `git push` seems
                      to silently hang with no output.


7. WORKING AGREEMENTS
------------------------
  - Push straight to `main` — that is the deploy flow. Confirm first if a
    change is large/risky; routine verified work does not need re-asking
    each time, per repeated explicit instruction from Johandre across many
    sessions.
  - Two-pass screenshots for any visual change (see section 5).
  - Keep this file updated after significant changes — it is meant to be
    perpetually current, not a one-time snapshot. If a gap forms (like the
    one between 2026-07-30 and 2026-08-06 that this update had to bridge),
    say so explicitly in the next update rather than pretending continuity
    that wasn't actually maintained.
  - `humanizer` skill runs on newly written user-facing copy.
  - Ask before fabricating any business fact, price, or content — see
    section 5.


8. HISTORICAL ARCHIVE (pre-2026-08-06 redesign — compressed, superseded)
------------------------------------------------------------------------
Everything below is from before the 2026-08-06 site-wide redesign. Design
system specifics (the old bark/pine/ember palette, Montserrat font, old
component names like WhyUs.tsx/PaymentPlan.tsx/CTABanner.tsx) NO LONGER
EXIST post-redesign — see section 2 for what replaced them. Kept only for
project archaeology (why certain content decisions were made, e.g. the FAQ
answers, the em-dash rule, the founder-unnamed rule, which all carried
forward unchanged).

Old sessions covered, in order: GA4 + consent banner setup; a full SEO
audit (scored 85/100, reports in seo-audit/); social preview images and
security headers; canonical-host fix (apex vs www); an em-dash removal pass
across all copy; a second blog article + new /insights layout; Google
Search Console verification tag; Google/Meta Ads "Growth" services added to
/services; a dedicated /faq page split out from the inline accordion; a
full-bleed homepage redesign around client-supplied forest photography
(later itself superseded by the 2026-08-06 redesign); the studio's
relocation from Brackenfell/Cape Town to George, Garden Route, in copy and
schema; and a payment-band reinstatement after an internal design critique.

Old environment gotchas that may still be relevant (not re-verified this
session, but worth knowing):
  - git push credential-manager hang: `GIT_TRACE=1 GIT_CURL_VERBOSE=1`
    showed a stall inside `git credential-manager get`. Fix used then:
    `git -c credential.helper= -c credential.helper=store push origin main`
    (the empty value is required — credential.helper is multi-valued).
  - Commit messages via a Bash tool: use a heredoc, not a PowerShell
    here-string (@'...'@ is invalid in Bash and silently produced a
    garbage commit subject once).
  - Satori (the OG-image renderer, app/_og/card.tsx or similar) cannot
    parse WOFF2 fonts — must stay TTF — and does not resolve CSS custom
    properties, only literal hex.

Full detail for all of the above, if ever needed, is in this file's git
history (the pre-2026-08-11 version) and in `GoodGround_memory.md` if that
file still exists in the repo.

===============================================================================

§16 — Sitewide scroll-stuck-halfway bug fix (2026-08-11, commit fb5e268)

Johandre reported he could only scroll about halfway down every page before
it stopped. Confirmed live on production: document.documentElement.scrollHeight
was 8900 but window.scrollTo(0, 8000) only settled around scrollY 5026.

Root cause: components/motion-gsap/LenisProvider.tsx creates a Lenis
instance with default autoResize: true, which watches
document.documentElement via a ResizeObserver. That observer does not fire
when page content grows the document's scrollHeight without the <html>
element's own box size changing (confirmed by reading
node_modules/lenis/dist/lenis.mjs directly). Lenis's cached scroll limit
falls behind the page's real height as content loads/expands, so scrolling
silently caps out partway down every page.

Fix: resync Lenis to GSAP ScrollTrigger's own "refresh" event (reliable —
re-measures on window load/resize and whenever any ScrollTrigger instance
changes) instead of relying on Lenis's own ResizeObserver alone. Added a
named handleResize function calling lenis.resize(), wired to
ScrollTrigger.addEventListener("refresh", handleResize) and
window.addEventListener("load", handleResize), with matching
removeEventListener calls in the useEffect cleanup (named references
required — inline arrows in both add/remove would silently fail to
unregister).

Verified via a local production build (npx next build && next start) using
Chrome DevTools MCP: scrolled both / and /pricing to their computed max
(scrollHeight - innerHeight) and confirmed scrollY settles at the target
on both. Note for future verification: Lenis intercepts window.scrollTo()
and animates toward the target over several rAF ticks rather than jumping
instantly, so a short polling-stability window (a few hundred ms) can
falsely report "stuck" while Lenis is still spinning up, especially on
heavier pages. Use a stability window of at least 1-2 seconds when
scripting this kind of check again.

npx tsc --noEmit and npx next build both clean before commit. Pushed to
main immediately after (fb5e268) — Vercel deploy should be live within a
few minutes of the push.

===============================================================================

§17 — SEO/UX checklist audit (2026-08-11, no code changes)

Johandre asked to check the site against a standard checklist: custom 404,
above-the-fold CTA on home, internal links, robots.txt, unique page titles,
meta descriptions, social share image, alt text, local schema. Full audit
via a research subagent, result:

  Custom 404            OK   app/not-found.tsx, fully on-brand, two CTAs
  CTA above fold         OK   Hero.tsx, "Let's Chat" -> /start-project
  Internal links          OK   articles.ts [label](/path) links + nav/footer
  robots.txt              OK   app/robots.ts, allows all, points at sitemap
  Unique page titles      OK   every route has its own, checked all of them
  Meta descriptions       OK   every route has its own, none duplicated
  Social share image      OK   dynamic per-route via opengraph-image.tsx
  Alt text on images      OK   descriptive everywhere; empty alts are all
                                genuinely decorative (watermark, OG internals)
  Local schema        PARTIAL  components/Schema.tsx HomeSchema() has name,
                                address (locality/region/country only),
                                areaServed, email, sameAs. MISSING:
                                streetAddress, postalCode, GeoCoordinates
                                (no lat/long anywhere in the codebase), and
                                phone is currently omitted since
                                content/site.ts site.phone is still null
                                pending Johandre confirming a number.

Only real gap is local schema. Next step if Johandre wants it closed: get a
phone number and a street address/postal code from him, then add a `geo`
GeoCoordinates block (lat/long for George) to HomeSchema() in
components/Schema.tsx. Not done yet — waiting on those two facts, per the
codebase's existing "wrong structured data is worse than absent structured
data" principle (see the TODO comment already in content/site.ts).

===============================================================================

§18 — Internal /brand-guide reference page (2026-08-11, commit c2fb128)

Johandre asked for a full in-depth brand guide page covering colors, fonts,
sizes, buttons, shadows, etc., as a no-index page. Built at
app/brand-guide/page.tsx, reachable only at /brand-guide (not linked from
nav or footer). metadata.robots: { index: false, follow: false, nocache:
true }.

Sections: brand overview (name/legal name/reg no/tagline, voice bullets),
logo (all 3 marks with correct backgrounds + usage rules), color (all 6
tokens with hex/CSS var/usage/contrast notes), typography (Clash Display +
Inter, full fluid type scale rendered live), layout & spacing (radius
scale, container widths, section padding rhythm), buttons (all 4 real
variants x 2 sizes, rendered from the actual Button.tsx component, not
mockups), shadow & elevation (soft/lift swatches), motion (easing curves +
durations), voice & tone (the site's hard copy rules), accessibility
(focus ring spec, contrast floor).

Deliberately pulls real values from globals.css/Button.tsx into the page
rather than hand-copying them a second time, so the guide can't quietly
drift out of sync with the actual design tokens the way a separate Figma
doc would.

Two issues caught and fixed during the two-pass screenshot verification
before shipping:
  1. The footer's CloudDivider scallop overlapped the last section's cards.
     Every page's last section before the footer needs pb-[24vw] reserved
     for it (see WhoWeBuildFor.tsx/CTABand.tsx/PricingPageClient.tsx for
     the established pattern) — missed this on the first pass since it's
     an easy thing to forget when a page isn't built from an existing
     section component.
  2. The page's own copy used em dashes in a few places, which directly
     contradicts the "no em dashes in user-facing copy" rule the page
     itself states in its Voice & Tone section. Swept and replaced with
     commas/periods/colons throughout before shipping.

npx tsc --noEmit and npx next build both clean. Verified at 1440/820/390
via Chrome DevTools MCP (full-page screenshots each breakpoint, no reflow
issues, footer scallop clears correctly at all three). Pushed to main
(c2fb128).

===============================================================================

§19 — Font swap: Syne (headings) + DM Sans (body) (2026-08-12, commit 2f77157)

Johandre asked to replace the site fonts with two Google Fonts: Syne for
headings, DM Sans for body (previously Clash Display Bold, a local .otf
file, and Inter).

app/layout.tsx: swapped the localFont(ClashDisplay) + Inter() calls for
Syne({ weight: "700" }) + DM_Sans() from next/font/google. Renamed the
underlying CSS variables from --font-clash-display/--font-inter to
--font-syne/--font-dm-sans in both layout.tsx and app/globals.css so the
token names stay accurate. Only 3 files referenced those raw variable
names (layout.tsx, globals.css, app/brand-guide/page.tsx as descriptive
text), so this was a clean rename. The semantic tokens every component
actually uses (--font-ht-display, --font-ht-body,
font-ht-display/font-ht-body Tailwind classes) were untouched — no
component files needed changes.

The one non-trivial part: app/_og/card.tsx (the Satori-based OG-image
renderer used by every opengraph-image.tsx route) needs its own
repo-local font FILE, separate from next/font/google's browser loading,
because Satori can't parse WOFF2 (what Google Fonts serves by default).
Downloaded a real Syne-Bold.ttf straight from Google's CDN: curl with a
legacy User-Agent header forces the css2 endpoint to return TTF links
instead of WOFF2 -

  curl -A "Mozilla/5.0 (Windows NT 6.1)" \
    "https://fonts.googleapis.com/css2?family=Syne:wght@700&display=swap"

- which returns a fonts.gstatic.com/.../....ttf URL to curl -o directly.
Saved as app/_og/Syne-Bold.ttf, replacing the old ClashDisplay-Bold.otf.
Deleted both old font files (app/_og/ClashDisplay-Bold.otf and
public/fonts/ClashDisplay-Bold.otf) since nothing references them anymore.
Worth remembering: this curl-with-legacy-UA trick is the fastest way to
pull a raw TTF/OTF straight from Google Fonts for any future Satori/OG
font needs, no GitHub-mirror hunting required.

Verified via local production build + Chrome DevTools MCP:
getComputedStyle on <h1>/<body> confirmed Syne/"DM Sans" are actually
applied (not silently falling back to a system font), checked the
homepage hero, /pricing's uppercase PageHero treatment, the /brand-guide
typography section, and the OG image route itself (/opengraph-image) to
confirm Satori renders Syne correctly end to end. All clean at desktop
and mobile. brand-guide/page.tsx's typography section copy also updated
to say Syne/DM Sans instead of Clash Display/Inter.

npx tsc --noEmit and npx next build both clean before commit. Pushed to
main immediately after (2f77157).

===============================================================================


§20 — Positioning fix + FAQ/Service schema + 6 INDUSTRY pages
       (2026-08-13, commit 4921a1b)
------------------------------------------------------------------------
An SEO review flagged four gaps.

  1. POSITIONING MISMATCH. Hero.tsx's H1 said "an AI-first digital agency"
     while every meta title, schema block and other page said "website
     development studio". Rewritten to lead with the studio wording and keep
     "AI-accelerated" as a differentiator inside the sentence. Grepped the
     whole app/ + components/ tree first: it was the only occurrence.
     STILL OUTSTANDING FOR JOHANDRE: update the Google Business Profile
     primary category and directory listings to match ("Website Designer" /
     "Web Developer"), and keep "AI-first" as on-page copy only, never as
     the GBP category.
  2. FAQPage schema existed only on the homepage. Added a reusable
     FAQSchema({items, id}) to components/Schema.tsx (the inline logic was
     extracted into a shared faqPageNode() helper so HomeSchema reuses it)
     and wired into /services, /faq and /pricing. /pricing uses its own
     separate pricingFaq set. RULE: schema must match what is VISIBLY
     rendered on that page, so /services marks up exactly the 4 questions it
     shows, not the whole list.
  3. Service schema: ServicesSchema({services}) maps content/services.ts
     into six Service nodes (@id = ${url}/services#slug). The existing
     ItemList now references those @ids instead of duplicating the data.
  4. SIX INDUSTRY PAGES at /services/<slug>: trades-and-home-services,
     hospitality-and-food, health-and-wellness, professional-and-consulting,
     retail-and-small-brands, non-profits-and-community. These are AUDIENCE
     segments (who we build for), distinct from the SERVICE pages in §22.
     New content/industryPages.ts holds per-segment copy; one shared
     components/sections/IndustryPageTemplate.tsx renders them; each route
     file is just metadata + the template.
     FAQAccordion.tsx was generalised to accept items/heading props
     (defaults preserve the existing homepage, /services and /faq behaviour
     exactly) so these reuse it rather than adding a third near-duplicate
     accordion.
     THE ACTUAL FIX for "named but never linked": WhoWeBuildFor.tsx's cards
     are now wrapped in Link (an href was added to each Industry entry).
     They were named on the homepage and linked nowhere.
     No client names, results or track record invented anywhere.


§21 — Blog article: "How much does a website cost in South Africa?"
       (2026-08-13, commit 9dd9407)
------------------------------------------------------------------------
~1,650 words, prepended to content/articles.ts (newest-first, so it is also
the featured post on /insights). Written to the Ogilvy-style brief Johandre
gave for the previous article (strict active voice, ruthless specificity, no
filler) and run through the humanizer checklist before shipping.
Real numbers throughout: the four packages R8,500-R32,000, the R6,000 Base
Build Fee, both payment options, and honest DIY-builder/freelancer/agency
market ranges framed as GoodGround's own estimate rather than attributed to
an invented source. Links into /pricing#packages and /pricing#build-your-own
(the configurator) plus /services#care-plans.
New category "Pricing" — categories are freeform strings in this codebase,
and as of §25 they now drive the /insights filter automatically.
NOTE: there is no ts-node here, so word count was verified with a throwaway
node script parsing the text/items fields between this entry's slug and the
next one's.


§22 — 6 dedicated SERVICE pages (2026-08-13, commit e133457)
------------------------------------------------------------------------
Distinct from §20's industry pages: these target individual SERVICES by
name. /services/website-care-plans, /google-ads, /meta-ads,
/website-redesign, /seo, /ux-design.
GOTCHA: the URL slugs do NOT all match content/services.ts slugs
(website-care-plans vs care-plans, seo vs seo-foundations). New
content/servicePages.ts has its own slug plus an optional serviceRef
pointing back at the services.ts slug, used for a "see it alongside every
other service" anchor into /services#<serviceRef>.
Rendered by components/sections/ServicePageTemplate.tsx, same thin-page
pattern as the industry template.
WEBSITE REDESIGN has no content/services.ts entry on purpose — it is the
website-development service applied to an existing site, not a separate
package, so it is kept out of the /services accordion but still gets its own
page, Service schema and sitemap entry.
Cross-linking: an optional relatedLink field was added to the Service type
in content/services.ts and populated for all six accordion rows;
ServiceAccordion.tsx renders it next to the CTA. website-development's
points at /services/website-redesign as the natural cross-sell.
SCOPE CONFIRMED WITH JOHANDRE BEFORE WRITING: GoodGround only maintains
sites it built, NOT other people's WordPress installs, even though
"WordPress maintenance South Africa" is real search demand. The care plans
page mentions WordPress as a contrast ("we build differently, no plugin
ecosystem to patch") rather than claiming to service WP sites. If asked to
write service or industry copy again, CHECK ACTUAL SCOPE before claiming a
capability just because it targets a good keyword.


§23 — Per-page OG/Twitter cards, FAQ de-dup, meta keywords + newsletter
       (2026-08-13, commit f7e1fe3)
------------------------------------------------------------------------
  - EVERY inner page's social share preview was showing the HOMEPAGE's title
    and description. Next.js does not derive openGraph/twitter from a page's
    own title/description: a page without its own openGraph silently
    inherits the root layout's static one. Confirmed live with curl before
    fixing. Fixed with lib/metadata.ts (pageSocialMeta({title, description,
    path}), which resolves the page's real effective title, unwrapping
    {absolute} or appending the layout's "%s | GoodGround" template) applied
    to all 21 page templates. The article template already had per-article
    openGraph but was missing twitter, so X shares specifically still leaked
    the homepage card.
  - Caught in passing: /faq's title was a plain string with "| GoodGround"
    hand-typed in, which the layout template would then append a SECOND time.
    Converted to {absolute}. Same pre-emptively on /pricing and /services.
  - FAQ DUPLICATE CONTENT: home and /services both rendered faq.slice(0,4),
    the same 4 questions verbatim on two indexed URLs, producing overlapping
    FAQPage schema. /services now shows a different, page-relevant slice
    selected BY QUESTION TEXT rather than array index, so reordering
    content/faq.ts cannot silently break it.
  - Removed the meta keywords tag from app/layout.tsx (ignored by search
    engines since the 2010s). NOTE the keywords field inside the article
    BlogPosting JSON-LD is a legitimate schema.org property, not an HTML meta
    tag — it was correctly left alone.
  - Removed the footer newsletter form (components/NewsletterForm.tsx
    deleted). It was intentionally disabled with no email list connected.
    Johandre chose "remove" over "wire it up" when asked. Re-add once there
    is an actual ESP.
  - Confirmed /sitemap.xml is well-formed (correct declaration, correct
    application/xml content-type, correct namespace, balanced tags). Next's
    MetadataRoute.Sitemap generates this safely; this was a confirmation, not
    a fix.


§24 — SITEWIDE ACCESSIBILITY AUDIT: contrast 96 -> 100
       (2026-08-13, commit 751f318)
------------------------------------------------------------------------
Johandre asked for a full pass "from a text, design, motion, transition and
SEO perspective". Audited the RUNNING PRODUCTION BUILD with Lighthouse and
performance traces rather than reading code, which is what surfaced the real
issues.
CONCLUSION UP FRONT: the site did not need a redesign. Baseline was already
LCP 510ms, CLS 0.00, SEO 100, Best Practices 100, zero console errors. The
changes were corrective.
The one real problem was three systemic WCAG AA contrast failures. The rules
that came out of it now live in SECTION 2 above — read them there, they are
the durable output of this work.
Also fixed an h1 -> h3 skip on /services (ServiceAccordion rows are h3 and
that page had no section heading above them; the homepage never showed it
because WhatWeBuild supplies its own h2). Added "Everything we build, in
detail."
Result: accessibility 100 on homepage, /services, /pricing, an article, an
industry page and /start-project.
Other fixes: replaced Tailwind's animate-bounce on the hero scroll cue with
a custom scroll-cue keyframe (bounce easing is banned by the project's own
design skill). NOTE Tailwind v4 puts -translate-x-1/2 on the standalone
translate property, so a keyframe animating transform composes with it
rather than clobbering the centring — verified, offset was -0.1px.

*** ROUTE TRANSITIONS: BUILT, MEASURED, REVERTED. DO NOT RE-ADD BLINDLY. ***
An app/template.tsx cross-page fade was built (opacity-only on purpose,
because /pricing's mobile summary bar is position:fixed inside the page tree
and a transform on the wrapper would make it a containing block). Then
traced: homepage LCP went 640ms -> 1086-1200ms across repeated runs, because
a wrapper animating from opacity 0 stops Chrome counting the hero as
painted. Doubling the headline metric for a decorative fade is a bad trade
on a site whose pitch is speed. Backed out; reasoning is recorded in
globals.css.
If revisited: the animation must be suppressed on FIRST LOAD, and note that
template.tsx REMOUNTS on every navigation, so a useRef "is first render"
guard inside it always reads true. The state has to live in layout.tsx.

PRODUCT.md WAS BADLY STALE and has been rewritten. It was still describing
the pre-2026-08-06 cream/forest-green palette, Sora + Inter typography and
the retired single "12 monthly payments" pitch. Since /impeccable and every
AI session load it as ground truth, auditing against it blindly would have
flagged the entire live design as off-brand. CHECK PRODUCT.md AGAINST THE
CODE BEFORE TRUSTING IT.


§25 — Category filtering on /insights (2026-08-13, commit 4864afc)
------------------------------------------------------------------------
Filter pills (All / Websites / Growth / Pricing) with per-category counts,
derived from the articles themselves so a new category appears
automatically. New components/InsightsList.tsx (client) plus a thinned
app/insights/page.tsx.
SEO CONSTRAINT: filtering is client-side and the DEFAULT STATE RENDERS EVERY
ARTICLE, so the HTML a crawler receives still contains the full list. No
article sits behind an interaction. Verified by curling the server HTML.
PAYLOAD: the page passes a trimmed ArticleSummary shape, not articles
straight through — the full array would serialise every article's body
blocks into the RSC payload for a listing that shows only titles and
excerpts.
The featured card shows only on the unfiltered list, where "newest" means
something.
BUG FOUND AND FIXED DURING BUILD: the scroll-reveal sets children to
opacity 0 and only tweens them back when the container scrolls into view.
Correct on first load, WRONG for filtering — clicking "All" pushed results
below the fold where they sat invisible, so the click looked like it did
nothing. Results now skip the reveal once the visitor has filtered
(hasFiltered state swaps the wrapper). Verified across all five transitions
including the round trip back to All: zero invisible cards, and the
first-load animation still runs.
44px tap targets, group labelled, aria-pressed, aria-live result count.


§26 — /work rebuilt as a project list + the live self-measuring panel
       (2026-08-13, commit 24bfd6c)
------------------------------------------------------------------------
/work was an apology sitting in the main nav ("case studies, coming soon"),
which is the page a buyer opens to answer "can these people actually build?".
Now a real project list driven by content/projects.ts — see OPEN ITEM 0,
Johandre is adding real projects within the week and that is a content-only
change.
PROJECT 01 is the studio's own site, and instead of a screenshot it renders
components/LiveMetrics.tsx: a readout taken from the VISITOR'S OWN BROWSER
via the Performance APIs (LCP, CLS, TTFB, page weight), graded against
Google's published thresholds. Nothing is hard-coded or fetched from us; a
visitor can verify every number in their own devtools. The pitch is "we'd
rather be tested than believed", which is the honest answer for a studio
with no handed-over client work yet.

THREE BUGS CAUGHT BY TESTING, ALL INSTRUCTIVE — do not reintroduce:
  1. CLS IS NOT THE SUM OF EVERY SHIFT. That is the obvious-looking and
     wrong implementation. Google scores the largest SESSION WINDOW (a run
     of shifts each within 1s of the previous and 5s of the first). The
     naive sum reported 0.47 on a page Chrome's own trace scored 0.00,
     because a browser resize counted. The panel built to vouch for the site
     was libelling it. Now implements the real session-window algorithm.
  2. HYDRATION MISMATCH. Tiles were rendered conditionally on
     PerformanceObserver support, which is false on the server and true in
     Chrome: three tiles server-side, four after hydration. React error #418,
     Best Practices dropped to 96, and a tile appearing from nowhere would
     itself shift layout. Everything is now derived from result state (null
     on both server and first client render) and NEVER from calling
     supports() during render.
  3. FALSE GOOGLE CLAIM. Where a browser does not report LCP (Safari,
     Firefox, and any page Chrome loaded backgrounded) the panel falls back
     to DOMContentLoaded — but was still captioned "Google rates under 2.5s
     as good". That threshold is defined for LCP specifically. The fallback
     now says the browser does not report it and drops the attribution.
THE PANEL MUST NOT SHIFT THE PAGE IT GRADES: every value slot reserves its
height and the tile count is fixed. Verified at 0 shift while populating.
components/AnimatedNumber.tsx gained an optional format prop (defaults to
rand, so all pricing call sites are untouched) rather than adding a second
count-up. Pass a STABLE function reference — it is in the effect deps, so an
inline arrow would restart the tween every render.
Phone and WhatsApp work landed in this same commit — see section 6.
Lighthouse on /work: 100 accessibility, best practices, SEO.


§27 — Homepage promoted from /home-test-2 + font swap to Parkinsans/
      Instrument Sans (2026-08-20)
------------------------------------------------------------------------
The homepage was rebuilt as an A/B variant at /home-test-2, then promoted
to be the live homepage on 2026-08-20 after client approval, replacing the
section set §20-26 above describe (Hero.tsx, ServiceCarousel.tsx,
CreativeProcess.tsx, SixReasons.tsx, WhatWeBuild.tsx, WhoWeBuildFor.tsx,
About.tsx). The /home-test-2 route itself no longer exists — it was
promoted, not kept as a separate page. Current homepage sections live under
components/hometest2/ (app/page.tsx assembles them). Old components/sections/
files with the same names as before were left in place until the
2026-08-23 cleanup (§28) deleted them once confirmed dead.

Same day: fonts changed from Syne + DM Sans (§19) to Parkinsans (headings)
+ Instrument Sans (body), client-supplied choice. Section 2 above has been
updated in place to reflect this. The one non-trivial part, same shape as
the Syne swap: Satori (the OG-image renderer) needs its own static-weight
TTF file, separate from next/font/google's browser loading, because Google
only serves Parkinsans as a variable font and Satori renders a variable
font at its default axis position (300/Light) rather than the needed 700
(Bold) unless given a pinned static instance. Generated via fontTools from
the variable font and saved to app/_og/Parkinsans-Bold.ttf.


§28 — Em-dash sweep, dead-code cleanup, first DESIGN.md (2026-08-23)
------------------------------------------------------------------------
Three-part cleanup pass, done in this order:

1. CONTENT FIX: em dashes had crept back into user-facing copy (mainly
   content/caseStudies.ts, 20 instances, plus one each in app/legal,
   app/pricing, app/services) despite the standing "no em dashes" rule.
   Each replaced with contextually appropriate punctuation (comma, colon,
   parentheses, period) chosen per sentence, not a blind find-replace. Code
   comments are exempt from this rule and were left alone.

2. CLEANUP: deleted genuinely dead code left over from the pre-2026-08-20
   homepage (§27) — components/sections/About.tsx, CreativeProcess.tsx,
   Hero.tsx, ServiceCarousel.tsx, SixReasons.tsx, WhatWeBuild.tsx,
   WhoWeBuildFor.tsx, components/motion/RotatingWords.tsx (the old Hero's
   specialty-word cycler, no longer used anywhere), plus 8 now-orphaned
   images. Verified dead via exact import-path grep, not just filename
   matching, since components/hometest2/sections/ has files with the SAME
   NAMES as the deleted ones in components/sections/ — a name-only check
   would have wrongly flagged live files as dead. Also removed a dead
   `@keyframes scroll-cue` block from globals.css (only the deleted Hero.tsx
   used it) and fixed a stale PRODUCT.md line about which files still use
   framer-motion.

3. DESIGN.md CREATED for the first time (project root, plus a
   .impeccable/design.json sidecar), via `/impeccable document` scan mode,
   generated from the real implemented tokens/components rather than
   invented. PRODUCT.md's old "No DESIGN.md" known-gap line is now stale
   and should be removed next time that file is touched. Covers Colors,
   Typography, Layout, Elevation & Depth (names "the Offset-Edge Rule" as
   the signature depth device), Shapes, Components, Do's and Don'ts — all
   grounded in real audit history from this file's own §24 contrast fixes.


§29 — Google Business Profile confirmed real and mostly healthy (2026-08-24)
------------------------------------------------------------------------
Johandre supplied the real GBP Maps link this session. This overturns the
SEO audit's long-standing assumption that no GBP existed yet (open item 2
above, and item 12 in seo-audit/ACTION-PLAN.md, are now resolved/stale).
Verified live via Chrome DevTools MCP navigating Google Maps directly and
reading its accessibility tree (WebFetch cannot render Maps' JS content,
returns only the bare page shell):

  - Phone (067 010 4988) matches what's already in content/site.ts/schema —
    no NAP mismatch on that front.
  - Pin location initially looked wrong (near Gqeberha, Eastern Cape, at a
    wide zoom — roughly 500km from George). CONFIRMED FALSE ALARM by
    Johandre: he set the GBP service area to all of South Africa rather
    than just George, so Google auto-assigned an arbitrary pin somewhere in
    that area. Not a real NAP problem — do not re-flag this from a future
    Maps screenshot without this context.
  - Hours are WRONG: "Open 24 hours" Mon-Fri, "Closed" Sat/Sun — looks like
    an unset dashboard default. OPEN ITEM, needs Johandre to fix in the GBP
    dashboard directly (see open item 6 above).
  - Category is "Design agency" — worth him checking for a more precise
    option (open item 6).
  - A local post is already live, linking to /work/point-break-surf.


§30 — SEO audit re-run: FAQ dedup, video lazy-load, cookie-banner overlap,
      framer-motion removed from root layout (2026-08-24, commit bf6e0ec,
      pushed/live)
------------------------------------------------------------------------
Full-site SEO audit re-run (10 parallel subagents). Several subagents'
first report was truncated mid-task (shared ~34-41M ms duration_ms across
different agents, looked like a shared execution ceiling) — recovered every
time by SendMessage to the bare agent ID telling it to finish and re-post
the complete report. IF A FUTURE SEO SUBAGENT'S FIRST NOTIFICATION READS
LIKE DEBUGGING NARRATION RATHER THAN A REPORT, IT'S TRUNCATED — RESUME IT,
DON'T REPORT THE FRAGMENT AS A FINDING.

Four fixes shipped from this round, all in commit bf6e0ec:

  1. HOMEPAGE FAQPAGE SCHEMA DUPLICATION: app/page.tsx had re-grown a
     direct <FAQSchema> call for an 8-question subset that /faq already
     covers canonically with the full list — the exact same duplication
     class fixed once before between HomeSchema and /faq (§23), still
     correctly documented in Schema.tsx's own comment, but a DIFFERENT file
     independently reintroduced the pattern. Removed the duplicate call and
     its now-unused const/imports from app/page.tsx. LESSON: a "don't do
     this again" code comment only prevents a repeat in the file it's
     written in, not in a different file that reintroduces the same
     mistake independently.
  2. CASE-STUDY VIDEO EAGER-LOAD: app/work/[slug]/page.tsx's
     <video autoPlay muted loop> had no `preload` attribute, defaulting to
     preload="auto" and downloading eagerly on page load despite being
     well below the fold. New components/LazyLoopVideo.tsx (client
     component, IntersectionObserver + preload="none", only sets `src`/
     `autoPlay` once within 200px of viewport) fixes it and is reusable if
     another case study gets a video.
  3. COOKIE BANNER COVERING THE MOBILE STICKY CTA BAR: both
     components/Analytics.tsx's consent banner and
     components/hometest2/MobileStickyBar.tsx (the "Start your project" /
     WhatsApp bar) are `fixed bottom-0` on mobile. The banner's higher
     z-index and greater height meant it fully hid the sticky bar
     underneath whenever both were visible (confirmed visually via
     screenshot — measured sticky-bar height 70.5px). Fixed by offsetting
     the banner to bottom-[78px] on mobile, reverting to bottom-0 at md+
     (where the sticky bar doesn't render, md:hidden). Also added
     env(safe-area-inset-bottom) padding to both, for iOS home-indicator
     clearance, since neither had it.
  4. SITEWIDE FRAMER-MOTION COST: Analytics.tsx lives in the ROOT LAYOUT
     (rendered on every page) and was importing framer-motion purely for
     the cookie banner's enter/exit animation — meaning the library shipped
     to every single page on the site, not just the two (/about, /pricing)
     that actually need it per PRODUCT.md. Replaced with the site's
     existing CSS `step-in` keyframe (already defined in globals.css,
     already used elsewhere) via a plain conditional-render div. No exit
     animation on dismiss (imperceptible trade for a lighter bundle
     sitewide). Also removed a now-fully-dead `<noscript>` CSS rule in
     app/layout.tsx that targeted the `data-reveal` attribute this
     component used to carry (nothing else in the codebase uses that
     attribute).

LCP WAS INVESTIGATED, FOUND HEALTHY, NOT "FIXED": the audit's stale
finding blamed a framer-motion hero animation for a CLS/TBT regression,
but that hero (and its framer-motion usage) was replaced in the 2026-08-20
promotion (§27) and no longer exists. Measured directly via Chrome
DevTools MCP performance traces instead of trusting the report: unthrottled
LCP 506ms (matches PRODUCT.md's ~510ms documented baseline almost exactly),
4x-CPU + Fast-4G-throttled LCP 1226ms — both comfortably inside Google's
"Good" threshold (<2.5s), CLS 0.00 both times. LESSON: verify a CWV claim
against a real trace before "fixing" it — a static SEO audit report can
describe an architecture that has since been replaced.

Verification: npx tsc --noEmit clean, npx next build clean (66 routes),
local production build (next start) tested via Chrome DevTools MCP —
screenshot confirms the sticky bar is now visible below the cookie banner
instead of hidden behind it, zero console errors on / and /pricing (the
one other framer-motion consumer, to confirm removing it from Analytics.tsx
didn't break the two pages that still need it). git push succeeded on the
first plain retry, no credential hang this time.

STILL OPEN from this audit round, not started: pricing-visibility-on-
homepage (open item 5 above — the one item Johandre flagged as an actual
business regression, not just a metric) and the GBP dashboard fixes
(open item 6, §29).
